RAG MODEL Tasks

Answer questions about user profiles (skills, commitment level, availability)
# node will genereate sql commands to get the data from postgres
# python will execute the sql commands and return the data to the node
# node will use the data to answer the user's question

Retrieve and surface project descriptions, timelines, and current status

Answer questions about team history, roles, and contributions


Provide context-aware streak quotes matched to the user's current activity domain
# system prompt for why i was matched for the project:Retrieve past feedback and ratings to inform team matching answers
Answer intelligent questions using platform data instead of hallucinating generic answers
1-Answer "why was I matched to this project?" using profile and project data
2-Answer questions from community threads
3-Retrieve and explain task history and workload distribution for a team



Generate project documentation from retrieved project context
Generate README content by retrieving project data (description, stack, contributors, milestones) !


feat:Summarize a project's full history on demand (tasks, milestones, activity)




| Feature | description
|1| Matching algorithm | Weighted 6-factor scoring (business-defined weights) |
|2| Persona handling | Student vs Early-career — different tone, depth, focus |
|3| Language | English + Arabic (auto-detected) |
|4| Workflow nodes | 5 nodes: onboarding, team_formation, phase_transition, blocker, milestone_warning |
|5| Phase transition | Validates deliverable checklist before allowing advancement |
|6| Team gap check | Warns on missing technical or non-technical roles |
|7| Session tracking | Persists persona, language, phase across conversation turns |
|8| Freemium logic | Agent aware of free vs premium limits, mentions naturally |
|9| Rating in matching | Yes — 20% weight in scoring |
---

## Architecture: Workflow Nodes

The agent routes every message through one of 5 workflow nodes based on what the user needs at that moment. Each node has its own retrieval strategy, system prompt instructions, and response behavior.

```
User Message
     │
     ▼
┌─────────────────────────────────────────────────────────┐
│                  WORKFLOW NODE DETECTOR                  │
│                                                         │
│  Keywords → Route to one of:                            │
│                                                         │
│  1. ONBOARDING       → "where do I start", "new here"   │
│  2. TEAM_FORMATION   → "find teammate", "need a dev"    │
│  3. PHASE_TRANSITION → "next phase", "done with MVP"    │
│  4. BLOCKER          → "stuck", "not responding"        │
│  5. MILESTONE_WARNING→ "behind", "overdue", "late"      │
│  6. GENERAL          → everything else                  │
└────────────────┬────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────┐
│                   RAG RETRIEVAL                          │
│                                                         │
│  Node-specific retrieval:                               │
│  • onboarding      → onboarding docs + persona guide    │
│  • team_formation  → user profiles (weighted) + rules   │
│  • phase_transition→ lifecycle docs for current phase   │
│  • blocker         → best_practices + community docs    │
│  • milestone_warning→ lifecycle + sprint docs           │
└────────────────┬────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────┐
│             PERSONA + LANGUAGE DETECTION                 │
│                                                         │
│  Persona: student | early_career | educator | company   │
│  Language: English | Arabic (auto from message)         │
└────────────────┬────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────┐
│              LLM (Claude Sonnet 4)                       │
│                                                         │
│  System prompt = persona guide + node instruction       │
│               + retrieved context                       │
│  Responds in user's language                            │
│  Grounded in KB only — no hallucination                 │
└────────────────┬────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────┐
│              PERSIST + RESPOND                           │
│                                                         │
│  • Save to chat_history with workflow_node tag          │
│  • Update session (persona, language, phase)            │
│  • Log milestone warnings if applicable                 │
│  • Return: reply + sources + workflowNode + language    │
└─────────────────────────────────────────────────────────┘
```

---

## Matching Algorithm

The matching engine scores every user profile across 6 factors using business-defined weights:

```
Final Score = (skill_complementarity × 0.35)
            + (availability_match    × 0.25)
            + (rating_history        × 0.20)
            + (phase_experience      × 0.12)
            + (learning_goals        × 0.05)
            + (domain_alignment      × 0.03)
```
